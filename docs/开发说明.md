# 开发与构建

本页面向希望构建、调试或修改 SPW Lyrics 的开发者。普通使用方法见 [使用说明](使用说明.md)。

## 技术栈

| 项目 | 当前配置 |
| --- | --- |
| 插件 | Kotlin/JVM 21 |
| SPW Workshop API | `0.1.0-dev21` |
| 手动搜索 / 批量窗口 | WinUI 3，.NET 8，Windows x64 |
| 构建 | Gradle Wrapper |
| 插件版本 | `0.4.0` |

`dev21` 分支面向 SPW 1.19.0，不兼容旧版 Workshop API。

## 构建

需要：

- JDK 21
- .NET 8 SDK
- Windows x64

在仓库根目录运行：

```powershell
.\gradlew.bat test plugin
```

插件包输出到：

```text
build/libs/plugin-spw-lyrics-0.4.0.spmod
```

构建 `plugin` 任务前会发布 WinUI 伴随程序。WinUI 使用自包含的 Windows App SDK 和 .NET 运行时，因此最终插件包不要求用户另外安装这些组件。

## 代码结构

核心代码按职责拆分，而不是按歌词平台堆在播放回调里。

| 目录 | 主要职责 |
| --- | --- |
| `domain` | 曲目查询、歌词候选、歌词文档、质量和匹配数据模型 |
| `codec` | TTML、QRC、KRC、YRC、LRC 的解析与 SPW 格式编码 |
| `provider` | AMLL、QQ、酷狗、网易云等来源适配 |
| `application` | 搜索、选择、缓存协调和批量处理 |
| `storage` | 歌词缓存、手动选择和本地数据布局 |
| `integration` | SPW Workshop API、设置、快捷键、播放回调和 WinUI 窗口接入 |

新增歌词来源时，优先实现独立 Provider 和格式适配，不要把平台请求逻辑直接写入播放回调或界面层。

## 歌词处理流程

自动搜索的大致流程为：

1. 各 Provider 搜索歌曲并返回统一的候选元数据。
2. 匹配器结合歌名、歌手、专辑和时长筛选候选，并排除明显的 Live、Remix、伴奏等冲突。
3. 下载候选歌词后再按实际内容判断是逐字、逐行还是普通歌词。
4. 自动选择先比较歌词质量；质量相同时再按来源优先级选择。
5. 保留所选来源自带的翻译和音译。
6. 最终歌词从首句开始编码为 SPW 接受的格式并写入缓存，再请求宿主刷新当前歌词。

自动播放采用渐进式结果：首轮最多等待约 10 秒取得可用结果，剩余来源继续在后台运行。后续如果找到更好的歌词，可以再次更新缓存和当前显示。手动选择不会被自动升级覆盖。

自动质量顺序为 `逐字 > 逐行 > 普通`。质量相同时的来源顺序为 `AMLL > QQ 音乐 > 酷狗音乐 > 网易云音乐`。

## 歌词来源

| 来源 | 获取方式 | 主要格式 / 数据 |
| --- | --- | --- |
| AMLL TTML DB | 缓存并查询索引，命中后下载单个 TTML 文件 | TTML、逐字、对唱、背景歌词 |
| QQ 音乐 | 搜索歌曲 ID 后获取并解码歌词 | QRC / LRC、逐字、翻译、音译 |
| 酷狗音乐 | 通过歌曲 Hash 查找歌词候选，再下载并解密 KRC | KRC、逐字、内嵌翻译 |
| 网易云音乐 | 搜索歌曲后优先请求新版歌词接口，失败时回退兼容路径 | YRC / LRC、翻译、音译 |
| SPW 本地歌词 | 不属于网络 Provider；插件没有合适结果或用户主动切回时交还 SPW | 内嵌歌词和同名 `.lrc` 由 SPW 处理 |

QQ 音乐、酷狗音乐和网易云音乐使用的是客户端或网页侧可访问的网络端点，并不是平台承诺长期稳定的开发者 API。接口地址、参数或返回结构发生变化时，对应 Provider 可能需要更新；单个来源失败不应阻止其他来源继续工作。

AMLL 只持久化索引并在命中后下载单个 TTML 文件，不会在运行时遍历整个歌词仓库。

## SPW 集成

`dev21` 使用 `com.xuncorp.spw.workshop` Gradle 插件，并申请：

- `library-read`
- `key-bindings`

曲库通过 `WorkshopApi.library.getAllTracks()` 读取，歌曲时长来自 `MediaItem.duration`。曲库 ID 只用于当前插件会话内补齐回调信息，不写入持久缓存。

手动搜索通过 `WorkshopApi.playback.getCurrentMediaItem()` 获取当前歌曲。快捷键由 `keyBindingManager` 注册，具体键位、冲突和全局绑定交给 SPW 管理。

当前 Workshop API 仍没有正式的“重新加载当前歌词”接口，因此自动刷新仍保留兼容桥。这个位置是 SPW 版本升级后最需要优先验证的部分之一。

## 批量处理

批量处理和正常播放共用同一套 `LyricsResolver` 与歌词缓存，但不触发播放刷新，也不会写回 SPW 音乐库。

当前最多同时处理 3 首歌曲。相同录音会根据曲名、歌手、专辑和近似时长复用解析结果，减少重复网络请求。进度由解析层报告，界面只负责选择范围和展示状态。

## 网络与本地数据

所有在线来源都先使用歌曲元数据进行搜索，只在需要检查或使用候选时下载歌词正文。插件不会读取或上传本地音频文件，也不要求用户登录音乐平台账号。

默认缓存位于：

```text
%LOCALAPPDATA%\SPW Lyrics\cache
```

用户手动选择和“切回本地歌词”也会保存到本地，用于覆盖全局自动策略。

## 参考项目

- [SPW 创意工坊 API](https://github.com/Moriafly/spw-workshop-api)：插件生命周期、设置、曲库和歌词加载接口。
- [AMLL TTML DB](https://github.com/amll-dev/amll-ttml-db)：AMLL TTML 歌词和元数据索引。
- [Lyricify Lyrics Helper](https://github.com/WXRIW/Lyricify-Lyrics-Helper)：QQ、酷狗、网易云接口及多种歌词格式实现参考。
- [Lyrico](https://github.com/Replica0110/Lyrico)：多来源歌词和元数据处理参考。
- [OpenCC4J](https://github.com/houbb/opencc4j)：跨字形匹配。
